// import React from 'react';
// import { Calendar, CalendarList, Agenda } from 'react-native-calendars';
// import CalendarEvent from './CalendarEvent';

// interface CalendarProps {
//   events: { name: string; date: Date; location: string }[];
// }

// const CustomCalendar: React.FC<CalendarProps> = ({ events }) => {
//   const renderEvent = ({ item }: { item: any }) => (
//     <CalendarEvent name={item.name} date={item.date} location={item.location} />
//   );

//   return (
//     // <Agenda items={events} renderItem={renderEvent} />
//   );
// };

// export default CustomCalendar;